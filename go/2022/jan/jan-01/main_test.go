package main

import "testing"

func TestNumberPicker(t *testing.T) {
	// nums := []int{1, 2, 3, 4}
	// probabilities := []float64{0.1, 0.5, 0.2, 0.2}
	// if number_picker(nums, probabilities) != 1 {
	// 	t.Error("Expected 1, got ", number_picker(nums, probabilities))
	// }
}
