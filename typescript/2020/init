#!/bin/zsh

now=$(date +%b-%d | tr '[:upper:]' '[:lower:]')

mkdir $now

echo "Created directory: $now"

cp ../../tsconfig.json $now

cd $now

cp -r ../../MochaStarter/* .
echo 'Installing dependencies ...'
npm i
code .
npm t
rm src/Basic.ts tests/Basic.test.ts
