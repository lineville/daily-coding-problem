import { expect } from 'chai';

describe('Basic', () => {

  beforeEach(() => {

  })

  
  xit('Hello', () => {
    const result : string = 'Hi Liam';
    expect(result).to.equal('Hi Liam');
  });

});
